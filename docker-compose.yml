version: "3.9"

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: issues

  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
